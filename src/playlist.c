#include "gba_helper.h"
typedef struct { u32 time; u8 lane; u16 note; } ChartNote;
typedef struct { const char* title; const ChartNote* chart; } Song;

const Song playlist[] = {
};

const int SONG_COUNT = 0;
